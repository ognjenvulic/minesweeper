import { createTheme } from '@mui/material/styles';

// A place to customize theme for this app
const theme = createTheme({
  palette: {
    //mode: 'dark',
    primary: {
      main: '#556cd6',
    },
  },
});

export default theme;